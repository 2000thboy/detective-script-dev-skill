{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Expert Review Result",
  "description": "Structured output from a review agent",
  "type": "object",
  "required": ["reviewer_id", "chapter", "dimensions", "overall_verdict", "provider", "model", "owner", "prompt_version", "created_at"],
  "properties": {
    "reviewer_id": {
      "type": "string",
      "description": "Agent identifier, e.g. logic-checker"
    },
    "chapter": {
      "type": "string",
      "description": "Chapter identifier, e.g. v1-ch07"
    },
    "dimensions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "verdict"],
        "properties": {
          "name": { "type": "string" },
          "verdict": {
            "type": "string",
            "enum": ["pass", "fail"]
          },
          "confidence": {
            "type": "string",
            "enum": ["high", "medium", "low"]
          },
          "reasoning": { "type": "string" },
          "evidence_quotes": {
            "type": "array",
            "items": { "type": "string" }
          },
          "action_items": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    },
    "overall_verdict": {
      "type": "string",
      "enum": ["pass", "needs_revision"]
    },
    "provider": { "type": "string" },
    "model": { "type": "string" },
    "owner": { "type": "string" },
    "prompt_version": { "type": "string" },
    "created_at": { "type": "string" },
    "derived_from_lock_hash": { "type": "string" },
    "critical_issues": {
      "type": "array",
      "items": { "type": "string" }
    },
    "summary": { "type": "string" }
  }
}
